apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: external-policy
  namespace: vcluster-private
spec:
  podSelector: 
    matchLabels: {}
  policyTypes:
  - Ingress
  ingress:
    - from:
      - podSelector: {}
            
